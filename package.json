{
  "name": "contextlens",
  "version": "1.0.3",
  "private": true,
  "description": "ContextLens - 100% Local-first AI developer context engine and persistent knowledge graph for VS Code.",
  "author": "Noventra-Labs",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Noventra-Labs/ContextLens.git"
  },
  "bugs": {
    "url": "https://github.com/Noventra-Labs/ContextLens/issues"
  },
  "homepage": "https://github.com/Noventra-Labs/ContextLens#readme",
  "workspaces": [
    "vscode-extension"
  ],
  "scripts": {
    "compile": "npm --prefix vscode-extension run compile",
    "watch": "npm --prefix vscode-extension run watch",
    "test": "npm --prefix vscode-extension run test:unit",
    "package": "npm --prefix vscode-extension run package",
    "package:vsix": "cd vscode-extension && npx --yes @vscode/vsce package --no-dependencies"
  }
}
